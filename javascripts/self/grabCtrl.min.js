app.controller("grabCtrl",["$scope","$http","Tool","Ajax",function($scope,$http,Tool,Ajax){
	$scope.queryParams = {
		dayDate:"",
		profressionId:2,
		city:"深圳",
		currentPage:1,
		area:"",
	}

	//数据加载动画
	$scope.hasBg = false;
	//菜单切换变量
	$scope.hasDate = false;
	$scope.hasArea = false;
	$scope.hasClass = false;
	$scope.hasBg = false;
	//菜单项变量
	$scope.menuParams = {
		area:false,
		classes:false,
		date:false,
	}
	$scope.areaParams = {
		default:true,
		futian:false,
		nanshan:false,
		luohu:false,
		baoan:false,
		longhua:false,
		longgang:false,
		yantian:false,
	}
	$scope.dateParams={};
	$scope.dates = [];
	$scope.classParams = {
		jieya:true,
		meirong:false,
		tuina:false,
	}

	$scope.load = function(){
		Ajax.loadHost();
		$scope.loadDate();
		$scope.checkUse();
	}

	//检查是否有浏览过该页面
	$scope.checkUse = function(){
		if(Tool.getLocal("used")){
			$scope.noUsed = false;
		}else{
			$scope.noUsed = true;
			Tool.setLocal("used","true");
		}
	}

	//隐藏图片提示
	$scope.hideTip = function(){
		$scope.noUsed = false;
	}

	//加载日期时间
	$scope.loadDate = function(){
		var url = Tool.getSession("host")+"/new/contents/json/date.json";
		$http.get(url)
		.success(function(data){
			for(var i in data){
				var item = data[i];
				if(i==0){
					$scope.dateParams[item.value] = true;
				}else{
					$scope.dateParams[item.value] = false;
				}
				$scope.dates.push(item.value);
			}
		}).error(function(){
			Tool.alert($scope,"时间加载异常，请稍后再试！");
		})
	}

	//切换下拉菜单
	$scope.switchMenu =function(menu){
		switch(menu){
			case "area":
				if($scope.hasDate){
					$scope.hasDate = false;
					$scope.menuParams.date = false;
				};
				if($scope.hasClass){
					$scope.hasClass = false;
					$scope.menuParams.classes = false;
				};
				$scope.hasArea = !$scope.hasArea;
				if($scope.hasArea){
					$scope.menuParams.area = true;
				}else{
					$scope.menuParams.area = false;
				}
				break;
			case "date":
				if($scope.hasArea){
					$scope.hasArea = false;
					$scope.menuParams.area = false;
				};
				if($scope.hasClass){
					$scope.hasClass = false;
					$scope.menuParams.classes = false;
				};
				$scope.hasDate = !$scope.hasDate;
				if($scope.hasDate){
					$scope.menuParams.date = true;
				}else{
					$scope.menuParams.date = false;
				}
				break;
			case "class":
				if($scope.hasDate){
					$scope.hasDate = false;
					$scope.menuParams.date = false;
				};
				if($scope.hasArea){
					$scope.hasArea = false;
					$scope.menuParams.area = false;
				};
				$scope.hasClass = !$scope.hasClass;
				if($scope.hasClass){
					$scope.menuParams.classes = true;
				}else{
					$scope.menuParams.classes = false;
				}
				break;
		}
		if($scope.hasDate||$scope.hasArea||$scope.hasClass){
			$scope.hasBg = true;
		}else{
			$scope.hasBg = false;
		}
	}

	//时间下拉菜单项点击
	$scope.dateSelect = function(params){
		Tool.select(params,$scope.dateParams);
	}

	//区域下拉菜单项点击
	$scope.areaSelect =function(params){
		Tool.select(params,$scope.areaParams);
	}

	//分类下拉菜单项点击
	$scope.classSelect = function(params){
		Tool.select(params,$scope.classParams);
	}

	//加载数据
	$scope.loadData(){
		var url = Tool.getSession("host")+"/wx/gift/queryproduct";
		var params = "dayDate="+$scope.queryParams.dayDate+"&profressionId="+$scope.queryParams.profressionId+"&city="+$scope.queryParams.city+"&area="+$scope.queryParams.area+"&currentPage="+$scope.queryParams.currentPage;
		$http.post(url,params,{
			headers: {
			   'Content-type':'application/x-www-form-urlencoded;charset=UTF-8',
			 }
		}).success(function(data){
			
		})
	}
}])
