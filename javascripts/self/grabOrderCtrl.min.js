app.controller("grabOrderCtrl",["$scope","$http","Tool",function($scope,$http,Tool){
	$scope.hasGift = false;
	$scope.gift = {};

	/*
	** 初始化页面
	*/
	$scope.init = function(){
		Tool.loadUserinfo($scope);
		$scope.loadGift();
	}

	$scope.loadGift = function(){
		if(Tool.getLocal("gift")){
			$scope.gift = Tool.getLocal("gift");
			$scope.hasGift = true;
		}
	}

	/*
	//查询接口暂不可用
	$scope.queryGift = function(){
		var url = Tool.getSession("host")+"/wx/order/queryGift";
		$http.get(url,{
			headers:{
				 'Content-type':'application/x-www-form-urlencoded;charset=UTF-8',
				 "accessToken":$scope.userInfo.accessToken,
			}
		}).success(function(data){

		})
	}*/
}])
